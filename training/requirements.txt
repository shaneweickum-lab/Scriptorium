# Core ML framework — PyTorch 2.1+ required for MPS scaled_dot_product_attention
torch>=2.1.0

# HuggingFace ecosystem — model serialisation and tokenizer
transformers>=4.40.0
tokenizers>=0.19.0

# Dataset utilities (Phase 2 data pipeline)
datasets>=2.19.0

# Numerics / progress
numpy>=1.24.0
tqdm>=4.66.0

# Testing
pytest>=7.4.0
