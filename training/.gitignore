# Model checkpoints and weights
checkpoints/
*.pt
*.pth
*.gguf
*.onnx
*.bin

# Tokenizer output (generated — not committed)
tokenizer/maven-tokenizer/

# Training data directories (large, not committed)
# The data/ Python package (scripts) IS committed; only ignore the actual data
data/raw/
data/clean/
data/tokenized/
data/instruction_pairs/

# Python artefacts
__pycache__/
*.pyc
*.egg-info/
dist/
.venv/
venv/
